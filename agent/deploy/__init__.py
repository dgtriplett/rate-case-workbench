"""MLflow registration + Mosaic Model Serving deploy scripts for each agent.

Run::

    python -m agent.deploy.register_all        # logs + registers all 4 agents
    python -m agent.deploy.deploy_endpoints    # creates/updates Model Serving endpoints
"""
